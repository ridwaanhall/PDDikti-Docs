---
title: Biaya Kuliah
description: Informasi range biaya kuliah pada suatu perguruan tinggi berdasarkan id_pt.
---

Dokumentasi ini menjelaskan cara menggunakan endpoint `Biaya Kuliah` dan bagaimana struktur response yang akan Kamu terima. Dengan endpoint ini, Kamu dapat melihat informasi terkait range biaya kuliah pada suatu perguruan tinggi berdasarkan `id_pt`.

## Endpoint

Untuk mendapatkan informasi range biaya kuliah pada suatu Perguruan Tinggi, Kamu dapat menggunakan endpoint berikut:

```plaintext
GET /pt/biaya-kuliah/<str:id_pt>/
```

Pada endpoint ini, `id_pt` adalah ID unik dari perguruan tinggi yang ingin dicari informasi biaya kuliahnya.

## Contoh Endpoint

Untuk memberikan gambaran yang lebih jelas, berikut adalah contoh penggunaan endpoint:

```plaintext
GET /pt/biaya-kuliah/IIrmU70D6TnWs79XzUmnBLmgfQLYxkZzUeQjVW4cA9_WKIIB0oJfhVSNS865-zzStoB4tg==/
```

> Dalam contoh ini, API akan mencoba mengambil informasi range biaya kuliah pada perguruan tinggi yang memiliki ID `IIrmU70D6TnWs79XzUmnBLmgfQLYxkZzUeQjVW4cA9_WKIIB0oJfhVSNS865-zzStoB4tg==`.

## Contoh Penggunaan

Berikut adalah 3 contoh penggunaan yang kami buat, yaitu menggunakan Python, PHP, dan akses API secara langsung.

<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
    <TabsTrigger value="php">PHP</TabsTrigger>
    <TabsTrigger value="direct">Direct API Access</TabsTrigger>
  </TabsList>
  <TabsContent value="python">

    Kamu dapat mengubah `id_pt` sesuai kebutuhan kamu.

    ```python:main.py showLineNumbers {13-14}
    import requests
    import json

    def fetch_api(api_url):
        try:
            response = requests.get(api_url)
            response.raise_for_status()
            return response.json()
        except requests.exceptions.RequestException as e:
            print(f"Error: {e}")
            return None

    url = "https://api-pddikti.vercel.app/pt/biaya-kuliah"
    id_pt = "IIrmU70D6TnWs79XzUmnBLmgfQLYxkZzUeQjVW4cA9_WKIIB0oJfhVSNS865-zzStoB4tg=="
    api_url = f"{url}/{id_pt}/"
    data = fetch_api(api_url)

    print(json.dumps(data, indent=4) if data else "No data fetched.")
    ```

    - **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    - **Menggabungkan URL**: `url` dan `id_pt` digabungkan untuk membentuk URL lengkap.
    - **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="php">

    Kamu dapat mengubah `id_pt` sesuai kebutuhan kamu.

    ```php:main.php showLineNumbers {15-16}
    <?php
    function fetch_api($api_url) {
        $ch = curl_init();
        curl_setopt($ch, CURLOPT_URL, $api_url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
        $response = curl_exec($ch);
        if (curl_errno($ch)) {
            echo 'Error: ' . curl_error($ch);
            return null;
        }
        curl_close($ch);
        return json_decode($response, true);
    }

    $url = "https://api-pddikti.vercel.app/pt/biaya-kuliah";
    $id_pt = "IIrmU70D6TnWs79XzUmnBLmgfQLYxkZzUeQjVW4cA9_WKIIB0oJfhVSNS865-zzStoB4tg==";
    $api_url = $url . '/' . urlencode($id_pt) . '/';
    $data = fetch_api($api_url);

    if ($data) {
        echo '<pre>' . json_encode($data, JSON_PRETTY_PRINT) . '</pre>';
    } else {
        echo "No data fetched.";
    }
    ?>
    ```

    - **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    - **Menggabungkan URL**: `url` dan `id_pt` digabungkan untuk membentuk URL lengkap.
    - **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="direct">
    Klik untuk mengakses [API directly](https://api-pddikti.vercel.app/pt/biaya-kuliah/IIrmU70D6TnWs79XzUmnBLmgfQLYxkZzUeQjVW4cA9_WKIIB0oJfhVSNS865-zzStoB4tg==/) atau copy text dibawah ini.

    ```plaintext
     https://api-pddikti.vercel.app/pt/biaya-kuliah/IIrmU70D6TnWs79XzUmnBLmgfQLYxkZzUeQjVW4cA9_WKIIB0oJfhVSNS865-zzStoB4tg==/
    ```

    - **Copy paste kode**: Copy kode dan paste pada browser untuk melihat API secara langsung.
  </TabsContent>
</Tabs>

## Response

Response ini berisi informasi range biaya kuliah pada perguruan tinggi berdasarkan ID yang diberikan.

<Tabs defaultValue="hasil" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="hasil">Hasil Response</TabsTrigger>
    <TabsTrigger value="struktur">Struktur JSON</TabsTrigger>
  </TabsList>
  <TabsContent value="hasil">
    ```json:response.json showLineNumbers
    {
        "id_sp": "MxUoHiNTPNZCIDIKT5ZycPWIWP9lr7ouaMNad3O9Zn7bQoSeYiMmGk9O2ILg0WNfLIChGw==",
        "nama_pt": "Universitas Teknologi Yogyakarta",
        "range_biaya_kuliah": "1.200.000 - 6.500.000"
    }
    ```
    - **Informasi Biaya Kuliah**: Berisi informasi terkait biaya kuliah, termasuk ID SP, nama PT, dan range biaya kuliah.
  </TabsContent>
  <TabsContent value="struktur">
    ```json:structure-response.json showLineNumbers
    {
        "id_sp": "string",
        "nama_pt": "string",
        "range_biaya_kuliah": "string"
    }
    ```
     - **Informasi Biaya Kuliah**: Berisi informasi terkait biaya kuliah, dengan tipe data string untuk ID SP, nama PT, dan range biaya kuliah.
  </TabsContent>
</Tabs>