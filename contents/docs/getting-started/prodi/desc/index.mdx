---
title: Deskripsi
description: Informasi deskripsi program studi berdasarkan id_prodi.
---

Dokumentasi ini menjelaskan cara menggunakan endpoint `Deskripsi Prodi` dan bagaimana struktur response yang akan Anda terima. Dengan endpoint ini, Anda dapat melihat informasi deskripsi program studi berdasarkan `id_prodi`.

## Endpoint

Untuk mendapatkan informasi deskripsi Program Studi, Anda dapat menggunakan endpoint berikut:

```plaintext
GET /prodi/desc/<str:id_prodi>/
```

Pada endpoint ini, `id_prodi` adalah ID unik dari program studi yang ingin dicari deskripsinya.

## Contoh Endpoint

Untuk memberikan gambaran yang lebih jelas, berikut adalah contoh penggunaan endpoint:

```plaintext
GET /prodi/desc/sIU3_7yvFq6ot-F8Hu3d1h0zWf21NkeT4wd-KWCCCFLo26jwAqZAMjDd_xDuloWA8CzNkg==/
```

> Dalam contoh ini, API akan mencoba mengambil informasi deskripsi program studi yang memiliki ID `sIU3_7yvFq6ot-F8Hu3d1h0zWf21NkeT4wd-KWCCCFLo26jwAqZAMjDd_xDuloWA8CzNkg==`.

## Contoh Penggunaan

Berikut adalah 3 contoh penggunaan yang kami buat, yaitu menggunakan Python, PHP, dan akses API secara langsung.

<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
    <TabsTrigger value="php">PHP</TabsTrigger>
    <TabsTrigger value="direct">Direct API Access</TabsTrigger>
  </TabsList>
  <TabsContent value="python">

    Kamu dapat mengubah `id_prodi` sesuai kebutuhan kamu.

    ```python:main.py showLineNumbers {13-14}
    import requests
    import json

    def fetch_api(api_url):
        try:
            response = requests.get(api_url)
            response.raise_for_status()
            return response.json()
        except requests.exceptions.RequestException as e:
            print(f"Error: {e}")
            return None

    url = "https://api-pddikti.vercel.app/prodi/desc"
    id_prodi = "sIU3_7yvFq6ot-F8Hu3d1h0zWf21NkeT4wd-KWCCCFLo26jwAqZAMjDd_xDuloWA8CzNkg=="
    api_url = f"{url}/{id_prodi}/"
    data = fetch_api(api_url)

    print(json.dumps(data, indent=4) if data else "No data fetched.")
    ```

    - **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    - **Menggabungkan URL**: `url` dan `id_prodi` digabungkan untuk membentuk URL lengkap.
    - **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="php">

    Kamu dapat mengubah `id_prodi` sesuai kebutuhan kamu.

    ```php:main.php showLineNumbers {15-16}
    <?php
    function fetch_api($api_url) {
        $ch = curl_init();
        curl_setopt($ch, CURLOPT_URL, $api_url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
        $response = curl_exec($ch);
        if (curl_errno($ch)) {
            echo 'Error: ' . curl_error($ch);
            return null;
        }
        curl_close($ch);
        return json_decode($response, true);
    }

    $url = "https://api-pddikti.vercel.app/prodi/desc";
     $id_prodi = "sIU3_7yvFq6ot-F8Hu3d1h0zWf21NkeT4wd-KWCCCFLo26jwAqZAMjDd_xDuloWA8CzNkg==";
    $api_url = $url . '/' . urlencode($id_prodi) . '/';
    $data = fetch_api($api_url);

    if ($data) {
        echo '<pre>' . json_encode($data, JSON_PRETTY_PRINT) . '</pre>';
    } else {
        echo "No data fetched.";
    }
    ?>
    ```

    - **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    - **Menggabungkan URL**: `url` dan `id_prodi` digabungkan untuk membentuk URL lengkap.
    - **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="direct">
    Klik untuk mengakses [API directly](https://api-pddikti.vercel.app/prodi/desc/sIU3_7yvFq6ot-F8Hu3d1h0zWf21NkeT4wd-KWCCCFLo26jwAqZAMjDd_xDuloWA8CzNkg==/) atau copy text dibawah ini.

    ```plaintext
    https://api-pddikti.vercel.app/prodi/desc/sIU3_7yvFq6ot-F8Hu3d1h0zWf21NkeT4wd-KWCCCFLo26jwAqZAMjDd_xDuloWA8CzNkg==/
    ```

    - **Copy paste kode**: Copy kode dan paste pada browser untuk melihat API secara langsung.
  </TabsContent>
</Tabs>

## Response

Response ini berisi informasi deskripsi program studi berdasarkan ID yang diberikan.

<Tabs defaultValue="hasil" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="hasil">Hasil Response</TabsTrigger>
    <TabsTrigger value="struktur">Struktur JSON</TabsTrigger>
  </TabsList>
  <TabsContent value="hasil">
    ```json:response.json showLineNumbers
    {
      "id_sms": "rjFQ1LGrOKOtVyH4wAGGv_fjoDlu7TGMq0s_X5pTNpAfQG7tv9JAWk3rjBVsPKrifGS49A==",
      "kode_prodi": "55202",
      "jumlah_dosen": 17,
      "jumlah_mahasiswa": 38,
      "jumlah_dosen_ajar": 0,
      "rasio": "-",
      "rasio_terima_daftar": "-",
      "akreditasi": "Unggul",
      "jumlah_pendaftar": 0,
      "jumlah_diterima": 0,
      "persentase": 0,
      "deskripsi_singkat": "",
      "visi": "",
      "misi": "",
      "kompetensi": "",
      "capaian_belajar": "",
      "rata_masa_studi": 4.833333
    }
    ```
    - **Deskripsi Prodi**: Berisi informasi deskripsi program studi, termasuk ID SMS, kode prodi, jumlah dosen, jumlah mahasiswa, jumlah dosen ajar, rasio, rasio terima daftar, akreditasi, jumlah pendaftar, jumlah diterima, persentase, deskripsi singkat, visi, misi, kompetensi, capaian belajar, dan rata-rata masa studi.
  </TabsContent>
  <TabsContent value="struktur">
    ```json:structure-response.json showLineNumbers
    {
        "id_sms": "string",
        "kode_prodi": "string",
        "jumlah_dosen": "number",
        "jumlah_mahasiswa": "number",
        "jumlah_dosen_ajar": "number",
        "rasio": "string",
        "rasio_terima_daftar": "string",
        "akreditasi": "string",
         "jumlah_pendaftar": "number",
        "jumlah_diterima": "number",
        "persentase": "number",
        "deskripsi_singkat": "string",
        "visi": "string",
        "misi": "string",
        "kompetensi": "string",
        "capaian_belajar": "string",
         "rata_masa_studi": "number"
    }
    ```
    - **Deskripsi Prodi**: Berisi informasi deskripsi program studi, dengan tipe data string untuk ID SMS, kode prodi, rasio, rasio terima daftar, akreditasi, deskripsi singkat, visi, misi, kompetensi, dan capaian belajar, serta number untuk jumlah dosen, jumlah mahasiswa, jumlah dosen ajar, jumlah pendaftar, jumlah diterima, persentase, dan rata-rata masa studi.
  </TabsContent>
</Tabs>