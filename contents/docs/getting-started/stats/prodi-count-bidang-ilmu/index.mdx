---
title: Program Studi Bidang Ilmu
description: Informasi jumlah total program studi terdaftar berdasarkan bidang ilmu.
---

Dokumentasi ini menjelaskan cara menggunakan endpoint `Statistik Jumlah Program Studi Berdasarkan Bidang Ilmu` dan bagaimana struktur response yang akan Kamu terima. Dengan endpoint ini, Kamu dapat melihat informasi jumlah total program studi terdaftar berdasarkan bidang ilmu.

## Endpoint

Untuk mendapatkan informasi jumlah total program studi terdaftar berdasarkan bidang ilmu, Kamu dapat menggunakan endpoint berikut:

```plaintext
GET /stats/prodi-count-bidang-ilmu/
```

Pada endpoint ini, tidak ada parameter yang diperlukan.

## Contoh Endpoint

Untuk memberikan gambaran yang lebih jelas, berikut adalah contoh penggunaan endpoint:

```plaintext
GET /stats/prodi-count-bidang-ilmu/
```

> Dalam contoh ini, API akan mencoba mengambil informasi jumlah total program studi terdaftar berdasarkan bidang ilmu.

## Contoh Penggunaan

<Note type="danger" title="perhatian!">
  Ada 3 contoh yang dapat kamu praktikkan sesuai kebutuhan. Python, PHP, dan Direct API Access (langsung ke browser).
</Note>

Berikut adalah 3 contoh penggunaan yang kami buat, yaitu menggunakan Python, PHP, dan akses API secara langsung.

<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
    <TabsTrigger value="php">PHP</TabsTrigger>
    <TabsTrigger value="direct">Direct API Access</TabsTrigger>
  </TabsList>
  <TabsContent value="python">

    Tidak ada parameter yang dibutuhkan.

    ```python:main.py showLineNumbers {13}
    import requests
    import json

    def fetch_api(api_url):
        try:
            response = requests.get(api_url)
            response.raise_for_status()
            return response.json()
        except requests.exceptions.RequestException as e:
            print(f"Error: {e}")
            return None

    url = "https://api-pddikti.ridwaanhall.com/stats/prodi-count-bidang-ilmu"
    api_url = f"{url}/"
    data = fetch_api(api_url)

    print(json.dumps(data, indent=4) if data else "No data fetched.")
    ```

    -   **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    *   **Menggabungkan URL**: `url` digabungkan untuk membentuk URL lengkap.
    -   **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="php">

    Tidak ada parameter yang dibutuhkan.

    ```php:main.php showLineNumbers {15}
    <?php
    function fetch_api($api_url) {
        $ch = curl_init();
        curl_setopt($ch, CURLOPT_URL, $api_url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
        $response = curl_exec($ch);
        if (curl_errno($ch)) {
            echo 'Error: ' . curl_error($ch);
            return null;
        }
        curl_close($ch);
        return json_decode($response, true);
    }

    $url = "https://api-pddikti.ridwaanhall.com/stats/prodi-count-bidang-ilmu";
    $api_url = $url . '/';
    $data = fetch_api($api_url);

    if ($data) {
        echo '<pre>' . json_encode($data, JSON_PRETTY_PRINT) . '</pre>';
    } else {
        echo "No data fetched.";
    }
    ?>
    ```

    -   **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    *   **Menggabungkan URL**: `url` digabungkan untuk membentuk URL lengkap.
    -   **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="direct">
    Klik untuk mengakses [API directly](https://api-pddikti.ridwaanhall.com/stats/prodi-count-bidang-ilmu/) atau copy text dibawah ini.

    ```plaintext
    https://api-pddikti.ridwaanhall.com/stats/prodi-count-bidang-ilmu/
    ```

    -   **Copy paste kode**: Copy kode dan paste pada browser untuk melihat API secara langsung.
  </TabsContent>
</Tabs>

## Response

Response ini berisi informasi jumlah total program studi terdaftar berdasarkan bidang ilmu.

<Tabs defaultValue="hasil" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="hasil">Hasil Response</TabsTrigger>
    <TabsTrigger value="struktur">Struktur JSON</TabsTrigger>
  </TabsList>
  <TabsContent value="hasil">
    ```json:response.json showLineNumbers
    [
         {
            "jumlah_prodi": 9583,
            "kelompok_bidang": "Pendidikan"
        },
        {
             "jumlah_prodi": 6638,
            "kelompok_bidang": "Sosial"
        },
         {
             "jumlah_prodi": 2447,
            "kelompok_bidang": "Agama"
         },
         {
            "jumlah_prodi": 1721,
            "kelompok_bidang": "MIPA"
         },
        {
            "jumlah_prodi": 6615,
            "kelompok_bidang": "Kesehatan"
        },
         {
            "jumlah_prodi": 2726,
           "kelompok_bidang": "Pertanian"
        },
        {
           "jumlah_prodi": 721,
            "kelompok_bidang": "Seni"
         },
         {
            "jumlah_prodi": 5845,
            "kelompok_bidang": "Ekonomi"
        },
         {
           "jumlah_prodi": 1115,
            "kelompok_bidang": "Humaniora"
        },
        {
            "jumlah_prodi": 8450,
            "kelompok_bidang": "Teknik"
        }
    ]
    ```
    -   **Jumlah Program Studi**: Berisi daftar informasi jumlah total program studi terdaftar berdasarkan bidang ilmu, termasuk kelompok bidang dan jumlah program studi.
  </TabsContent>
  <TabsContent value="struktur">
    ```json:structure-response.json showLineNumbers
    [
        {
            "jumlah_prodi": "number",
            "kelompok_bidang": "string"
         },
        ...
    ]
    ```
    -   **Jumlah Program Studi**: Berisi daftar informasi jumlah total program studi terdaftar berdasarkan bidang ilmu, dengan tipe data number untuk jumlah program studi dan string untuk kelompok bidang.
  </TabsContent>
</Tabs>