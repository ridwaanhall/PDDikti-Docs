---
title: Perguruan Tinggi Bentuk Lembaga
description: Informasi jumlah total perguruan tinggi terdaftar berdasarkan bentuk lembaga.
---

Dokumentasi ini menjelaskan cara menggunakan endpoint `Statistik Jumlah Perguruan Tinggi Berdasarkan Bentuk Lembaga` dan bagaimana struktur response yang akan Anda terima. Dengan endpoint ini, Anda dapat melihat informasi jumlah total perguruan tinggi terdaftar berdasarkan bentuk lembaga.

## Endpoint

Untuk mendapatkan informasi jumlah total perguruan tinggi terdaftar berdasarkan bentuk lembaga, Anda dapat menggunakan endpoint berikut:

```plaintext
GET /stats/pt-count-bentuk-pt/
```

Pada endpoint ini, tidak ada parameter yang diperlukan.

## Contoh Endpoint

Untuk memberikan gambaran yang lebih jelas, berikut adalah contoh penggunaan endpoint:

```plaintext
GET /stats/pt-count-bentuk-pt/
```

> Dalam contoh ini, API akan mencoba mengambil informasi jumlah total perguruan tinggi terdaftar berdasarkan bentuk lembaga.

## Contoh Penggunaan

Berikut adalah 3 contoh penggunaan yang kami buat, yaitu menggunakan Python, PHP, dan akses API secara langsung.

<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
    <TabsTrigger value="php">PHP</TabsTrigger>
    <TabsTrigger value="direct">Direct API Access</TabsTrigger>
  </TabsList>
  <TabsContent value="python">

    Tidak ada parameter yang dibutuhkan.

    ```python:main.py showLineNumbers {13}
    import requests
    import json

    def fetch_api(api_url):
        try:
            response = requests.get(api_url)
            response.raise_for_status()
            return response.json()
        except requests.exceptions.RequestException as e:
            print(f"Error: {e}")
            return None

    url = "https://api-pddikti.vercel.app/stats/pt-count-bentuk-pt"
    api_url = f"{url}/"
    data = fetch_api(api_url)

    print(json.dumps(data, indent=4) if data else "No data fetched.")
    ```

    -   **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    *   **Menggabungkan URL**: `url` digabungkan untuk membentuk URL lengkap.
    -   **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="php">

    Tidak ada parameter yang dibutuhkan.

    ```php:main.php showLineNumbers {15}
    <?php
    function fetch_api($api_url) {
        $ch = curl_init();
        curl_setopt($ch, CURLOPT_URL, $api_url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
        $response = curl_exec($ch);
        if (curl_errno($ch)) {
            echo 'Error: ' . curl_error($ch);
            return null;
        }
        curl_close($ch);
        return json_decode($response, true);
    }

    $url = "https://api-pddikti.vercel.app/stats/pt-count-bentuk-pt";
    $api_url = $url . '/';
    $data = fetch_api($api_url);

    if ($data) {
        echo '<pre>' . json_encode($data, JSON_PRETTY_PRINT) . '</pre>';
    } else {
        echo "No data fetched.";
    }
    ?>
    ```

    -   **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    *   **Menggabungkan URL**: `url` digabungkan untuk membentuk URL lengkap.
    -   **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="direct">
    Klik untuk mengakses [API directly](https://api-pddikti.vercel.app/stats/pt-count-bentuk-pt/) atau copy text dibawah ini.

    ```plaintext
    https://api-pddikti.vercel.app/stats/pt-count-bentuk-pt/
    ```

    -   **Copy paste kode**: Copy kode dan paste pada browser untuk melihat API secara langsung.
  </TabsContent>
</Tabs>

## Response

Response ini berisi informasi jumlah total perguruan tinggi terdaftar berdasarkan bentuk lembaga.

<Tabs defaultValue="hasil" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="hasil">Hasil Response</TabsTrigger>
    <TabsTrigger value="struktur">Struktur JSON</TabsTrigger>
  </TabsList>
  <TabsContent value="hasil">
    ```json:response.json showLineNumbers
    [
       {
            "jumlah_pt": 459,
            "nama_bentuk": "Politeknik"
        },
        {
            "jumlah_pt": 1402,
            "nama_bentuk": "Akademi"
        },
        {
             "jumlah_pt": 533,
             "nama_bentuk": "Institut"
        },
        {
            "jumlah_pt": 42,
            "nama_bentuk": "Akademi Komunitas"
        },
         {
             "jumlah_pt": 995,
            "nama_bentuk": "Universitas"
         },
        {
            "jumlah_pt": 3155,
            "nama_bentuk": "Sekolah Tinggi"
        }
    ]
    ```
    -   **Jumlah Perguruan Tinggi**: Berisi daftar informasi jumlah total perguruan tinggi terdaftar berdasarkan bentuk lembaga, termasuk nama bentuk dan jumlah perguruan tinggi.
  </TabsContent>
  <TabsContent value="struktur">
    ```json:structure-response.json showLineNumbers
    [
        {
            "jumlah_pt": "number",
            "nama_bentuk": "string"
        },
        ...
    ]
    ```
    -   **Jumlah Perguruan Tinggi**: Berisi daftar informasi jumlah total perguruan tinggi terdaftar berdasarkan bentuk lembaga, dengan tipe data number untuk jumlah perguruan tinggi dan string untuk nama bentuk lembaga.
  </TabsContent>
</Tabs>