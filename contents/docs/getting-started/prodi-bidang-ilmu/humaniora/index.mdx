---
title: Program Studi Bidang Ilmu Humaniora
description: Informasi jumlah mahasiswa dan persentase kelulusan program studi berdasarkan bidang ilmu Humaniora.
---

Dokumentasi ini menjelaskan cara menggunakan endpoint `Statistik Program Studi Berdasarkan Bidang Ilmu (Humaniora)` dan bagaimana struktur response yang akan Kamu terima. Dengan endpoint ini, Kamu dapat melihat informasi jumlah mahasiswa dan persentase kelulusan program studi berdasarkan bidang ilmu Humaniora.

## Endpoint

Untuk mendapatkan informasi jumlah mahasiswa dan persentase kelulusan program studi berdasarkan bidang ilmu Humaniora, Kamu dapat menggunakan endpoint berikut:

```plaintext
GET /prodi-bidang-ilmu/humaniora/
```

Pada endpoint ini, tidak ada parameter yang diperlukan.

## Contoh Endpoint

Untuk memberikan gambaran yang lebih jelas, berikut adalah contoh penggunaan endpoint:

```plaintext
GET /prodi-bidang-ilmu/humaniora/
```

> Dalam contoh ini, API akan mencoba mengambil informasi jumlah mahasiswa dan persentase kelulusan program studi berdasarkan bidang ilmu Humaniora.

## Contoh Penggunaan

<Note type="danger" title="perhatian!">
  Ada 3 contoh yang dapat kamu praktikkan sesuai kebutuhan. Python, PHP, dan Direct API Access (langsung ke browser).
</Note>

Berikut adalah 3 contoh penggunaan yang kami buat, yaitu menggunakan Python, PHP, dan akses API secara langsung.

<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
    <TabsTrigger value="php">PHP</TabsTrigger>
    <TabsTrigger value="direct">Direct API Access</TabsTrigger>
  </TabsList>
  <TabsContent value="python">

    Tidak ada parameter yang dibutuhkan.

    ```python:main.py showLineNumbers {13}
    import requests
    import json

    def fetch_api(api_url):
        try:
            response = requests.get(api_url)
            response.raise_for_status()
            return response.json()
        except requests.exceptions.RequestException as e:
            print(f"Error: {e}")
            return None

    url = "https://api-pddikti.vercel.app/prodi-bidang-ilmu/humaniora"
    api_url = f"{url}/"
    data = fetch_api(api_url)

    print(json.dumps(data, indent=4) if data else "No data fetched.")
    ```

    -   **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    *   **Menggabungkan URL**: `url` digabungkan untuk membentuk URL lengkap.
    -   **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="php">

    Tidak ada parameter yang dibutuhkan.

    ```php:main.php showLineNumbers {15}
    <?php
    function fetch_api($api_url) {
        $ch = curl_init();
        curl_setopt($ch, CURLOPT_URL, $api_url);
        curl_setopt($ch, CURLOPT_RETURNTRANSFER, 1);
        $response = curl_exec($ch);
        if (curl_errno($ch)) {
            echo 'Error: ' . curl_error($ch);
            return null;
        }
        curl_close($ch);
        return json_decode($response, true);
    }

    $url = "https://api-pddikti.vercel.app/prodi-bidang-ilmu/humaniora";
    $api_url = $url . '/';
    $data = fetch_api($api_url);

    if ($data) {
        echo '<pre>' . json_encode($data, JSON_PRETTY_PRINT) . '</pre>';
    } else {
        echo "No data fetched.";
    }
    ?>
    ```

    -   **Mengirim Permintaan**: Fungsi `fetch_api` mengirim permintaan GET ke URL API dan mengembalikan data JSON.
    *   **Menggabungkan URL**: `url` digabungkan untuk membentuk URL lengkap.
    -   **Mencetak Hasil**: Data yang diambil dicetak dalam format JSON yang rapi atau pesan kesalahan jika data tidak ditemukan.
  </TabsContent>
  <TabsContent value="direct">
    Klik untuk mengakses [API directly](https://api-pddikti.vercel.app/prodi-bidang-ilmu/humaniora/) atau copy text dibawah ini.

    ```plaintext
    https://api-pddikti.vercel.app/prodi-bidang-ilmu/humaniora/
    ```

    -   **Copy paste kode**: Copy kode dan paste pada browser untuk melihat API secara langsung.
  </TabsContent>
</Tabs>

## Response

Response ini berisi informasi jumlah mahasiswa dan persentase kelulusan program studi berdasarkan bidang ilmu Humaniora.

<Tabs defaultValue="hasil" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="hasil">Hasil Response</TabsTrigger>
    <TabsTrigger value="struktur">Struktur JSON</TabsTrigger>
  </TabsList>
  <TabsContent value="hasil">
    ```json:response.json showLineNumbers
    [
        {
            "nama_prodi": "Administrasi Fiskal",
            "total_mahasiswa": 260,
            "persentase_kelulusan": "48,46"
        },
        {
            "nama_prodi": "Akuntansi (Kampus Kab Jembrana)",
            "total_mahasiswa": 23,
            "persentase_kelulusan": " 4,35"
        },
        {
            "nama_prodi": "Akuntansi (Kampus Kab. Kepulauan Aru)",
             "total_mahasiswa": 516,
             "persentase_kelulusan": "25,58"
        },
        {
             "nama_prodi": "Akuntansi (Kampus Kab. Maluku Barat Daya)",
            "total_mahasiswa": 392,
            "persentase_kelulusan": "15,56"
        },
        {
            "nama_prodi": "Akuntansi (Kampus Kota Surabaya)",
             "total_mahasiswa": 180,
             "persentase_kelulusan": "43,33"
        },
        ...
    ]
    ```
    -   **Jumlah Mahasiswa**: Berisi daftar informasi jumlah mahasiswa dan persentase kelulusan program studi berdasarkan bidang ilmu Humaniora, termasuk nama program studi, total mahasiswa, dan persentase kelulusan.
  </TabsContent>
  <TabsContent value="struktur">
    ```json:structure-response.json showLineNumbers
    [
        {
            "nama_prodi": "string",
            "total_mahasiswa": "number",
            "persentase_kelulusan": "string"
        },
        ...
    ]
    ```
    -   **Jumlah Mahasiswa**: Berisi daftar informasi jumlah mahasiswa dan persentase kelulusan program studi berdasarkan bidang ilmu Humaniora, dengan tipe data string untuk nama program studi dan persentase kelulusan, serta number untuk total mahasiswa.
  </TabsContent>
</Tabs>